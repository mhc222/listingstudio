# DECISIONS.md — one line each

- 2026-08-28: Nested git repo inside home monorepo (like damdaryl app/) since brief says "init git repo" and this dir sits under the /Users/mattcronin repo.
- 2026-08-28: Full schema lands in Phase 1 (one migration) so later phases never block on migrations; edit_chain stored as ordered jsonb on file_groups rather than a join table — single-user tool, simpler reads.
- 2026-08-28: State machine fields live on file_groups (current_step, step_status, fal_request_id) instead of a separate steps table; revisit only if chains outgrow it (then log + propose Inngest/Trigger.dev per brief).
- 2026-08-28: MLS size presets applied at download time via sharp, not stored as extra objects — storage stays lean, presets are cheap to recompute.
- 2026-08-28: Supabase project created manually by Matt in org zzbpawcjzxujqxustqup (us-west-2), not via MCP in the DarylV3 org — PROGRESS.md's planned org/region superseded.
- 2026-08-28: HEIC decode via `heic-convert` (wasm libheif), not sharp — sharp prebuilt binaries exclude patent-encumbered HEIC; sharp still does dimension extraction and later resizing.
- 2026-08-28: PDF floor plans stored as-is with null dimensions and rendered as a link, not thumbnailed — avoids a PDF-render dependency for a single-user nicety.
- 2026-08-28: Photo grid uses plain `<img>` with signed URLs (1h expiry), not next/image — the optimizer caching signed URLs that expire causes broken images.
- 2026-08-28: fal called via raw fetch against the queue API, no @fal-ai/client dep — three endpoints, one auth header.
- 2026-08-28: Webhook auth = fal's ED25519 signature verified against their JWKS (node:crypto, fail closed, 5-min replay window); FAL_WEBHOOK_SECRET dropped from .env.example — fal has no shared-secret scheme.
- 2026-08-28: Local dev has no reachable webhook URL, so reconcile route doubles as the completion poller (`?all=1` ignores the 3-min cutoff); webhook param only sent when NEXT_PUBLIC_APP_URL is non-localhost.
- 2026-08-28: Generation-error auto-retry tracked via retry_count column (migration 0002), one retry then failed + re-run button; step outputs stored at deterministic path {fg}/step-{n}-r{retry} so duplicate completions upsert instead of duplicating objects.
- 2026-08-28: jobs.total_cost_cents widened to numeric (rates are fractional cents); incremented via security-definer rpc since supabase-js can't do relative updates.
- 2026-08-28: Parked idea (from flippin.ai scan): 5-sec before/after transformation video per output (one fal image-to-video call) as a social artifact — consider post-phase-10; no prompt reverse-engineering needed, their catalog is a subset of ours.
- 2026-08-28: Chained-step input resolved by listing outputs/{fg}/ for the newest step-{n-1}-r* object, not by computing the path from retry_count — the retry counter is per-group, so a retry on step N would otherwise point at a nonexistent previous-step path.
- 2026-08-28: Size presets enforced as a descending-JPEG-quality ladder (90→40) in the download route, no resizing — resolution-based MLS presets arrive with the phase 10 download menu.
- 2026-08-28: Reference images force the gemini provider — it's the only wired fal endpoint accepting multiple input images (qwen/kontext take a single image_url); refs are silently dropped elsewhere by design.
- 2026-08-28: Grounding computed once at job creation and stored in jobs.grounding_used ({dimension_sentence, floor_plan_photo_id}); orchestrator reads it per submit rather than re-deriving from rooms — the job record doubles as the "grounding used" audit per CLAUDE.md.
- 2026-08-28: VIRTUAL_RENOVATION already included in groundable/floor-plan-ref edit-type lists in the jobs route (a string match, not a stub) so phase 6 doesn't have to touch the route.
- 2026-08-28: Sample library has no delete UI — single-user tool, Supabase dashboard covers it; add when it actually annoys.
- 2026-08-28: DAY_TO_DUSK modeled as one edit type with a `preset` option (dusk default + 3 interior relight siblings) rather than 4 edit types — one template function, one picker entry; dusk QA checklist gated on preset=dusk.
- 2026-08-28: Dusk QA checkboxes are ephemeral client-side only (no persistence) — they are an eyeball aid until phase 8 auto-QA stores real verdicts.
- 2026-08-28: Interpreter uses the official @anthropic-ai/sdk (unlike fal's raw fetch) — one dep also serves phase 13 copywriting; model claude-haiku-4-5 with rates as data in config/models.ts.
- 2026-08-28: Interpreter ledger rows carry null job_id (the call precedes job creation; abandoned conversations still count) and are written via the admin client — RLS makes them invisible to user-scoped reads, so phase 15 spend aggregation must run server-side with the admin client.
- 2026-08-28: Chat conversation is ephemeral client state until a job is created, then persisted to chat_messages on the new file group; a question-only conversation that never becomes a job is not persisted (chat_messages requires a file_group_id).
- 2026-08-28: Interpreter defaults are "noted on the job record" as the assistant summary chat message ("Running: … Assumed: …") rather than a new jsonb column — visible in the thread, no migration.
- 2026-08-28: Rework modeled as an internal REWORK edit type appended to edit_chain (never user-pickable) — user reworks AND QA auto-retries reuse the whole state machine (idempotency, retries, realtime) instead of a second machine; ledger kind=rework.
- 2026-08-28: QA auto-retry cap = conditional qa_retry_count 0→1 transition (migration 0003); reset to 0 on each user rework so every rework cycle gets one auto-fix; "show best" = latest version displayed with its qa_note, all versions kept and switchable (no scoring comparison).
- 2026-08-28: QA never blocks delivery — an errored/unparseable QA call stores a "QA skipped" note and the version ships as passed.
- 2026-08-28: buildRework falls back to the raw user reaction as instructions if the Haiku call fails — a rework never hard-fails on interpreter errors.
- 2026-08-28: QA vision and rework-interpreter costs increment jobs.total_cost_cents (job exists by then), unlike pre-job intent-parsing calls which stay ledger-only with null job_id.
- 2026-08-28: Ideas spend = one upfront kind=ideas ledger row (Σ provider cost × chain length over the 4 variants); per-completion rows skipped for ideas fgs EXCEPT retries and reworks (extra calls are always counted, never double-counted).
- 2026-08-28: Ideas variants skip auto-QA (exploratory throwaways); a promoted variant gets QA through its rework cycle like any other output.
- 2026-08-28: Ideas labels live in file_groups.comment and are deliberately fed to the prompt as style language ("Warm Farmhouse") — no schema change, and the label doubles as useful prompt signal.
- 2026-08-28: Ideas promotion is client-side only (tap expands the variant into the normal before/after + rework UI) — every ideas fg already carries full version/rework machinery, so no backend "promote" exists.
- 2026-08-28: URL extraction parses HTML with regex (og:image + img tags, skip-list, 12-candidate cap), no parser dep; the min-size filter runs at import time via sharp (400px min) since candidate dimensions aren't knowable without fetching each image.
- 2026-08-28: sample_images.use_count incremented read-then-write, no rpc — single-user, races don't matter.
- 2026-08-28: Concurrency gate is a global running-count check before each claim plus kickQueued on freed slots + a cron sweep — best-effort ±1 under concurrent webhooks; it protects fal rate limits, not correctness, so no locking.
- 2026-08-28: Batch jobs skip room-dimension grounding when >1 photo (per-photo dims don't fit the single jobs.grounding_used record); the floor-plan ref still attaches to every group.
- 2026-08-28: Download `variant=original` means the untouched source photo; fg.size_preset "original" maps to variant "full" (full-res edited) — the name collision is resolved at the route, no schema change.
- 2026-08-28: Zip entries named {fg-id-prefix}-v{n}[-virtually-staged].jpg with the group's size preset and the staging-default watermark applied — no per-file toggles in the zip; use the single download for custom variants.
- 2026-08-28: FLOOR_PLAN_REDRAW runs as its own single-step chain on an is_floor_plan image (room photos, chained plan edits, and PDF inputs rejected at the jobs route); provider forced to gemini — the only wired endpoint rendering label/dimension text legibly.
- 2026-08-28: 3D plan variant = attach the finished 2D output as a listing floor plan, then redraw the attached plan with style=3d — no chained 2D→3D job (chains only version the final step, and attach is needed anyway for grounding).
- 2026-08-28: Address label + disclaimer are composited in code as white canvas bands (sharp SVG) at export/attach time, never prompted — AI-rendered fine print garbles; the flags still live on the redraw step's options so exports know what to apply.
- 2026-08-28: Plan SVG export wraps the raster PNG as an embedded image (no vectorization dep); PDF is letter-landscape fit-and-centered via pdf-lib.
- 2026-08-28: Plan redraws skip auto-QA — the QA prompt judges photo geometry, which a sketch→clean-plan redraw legitimately "violates"; reworks still ride the normal REWORK path.
- 2026-08-28: Tour panos live in the immutable originals bucket at {user}/tours/{tourId}/{sceneId}.{ext}; deleting a scene drops the row and deliberately orphans the object (storage policy forbids deleting originals) — single-user tool, storage is cheap.
- 2026-08-28: Scene upload rejects anything outside 2:1 ±0.1 — Marzipano equirect geometry assumes it, and a non-pano silently renders as a warped mess.
- 2026-08-28: Hotspots stored as jsonb on tour_scenes ({yaw, pitch, target, label}) rather than a hotspots table — they are only ever read as a whole scene, like edit_chain on file_groups.
- 2026-08-28: Public tour share page must read via the admin client (RLS blocks anon) and sign pano URLs server-side per request; middleware matcher must exempt /tour or the share link redirects to /login.
- 2026-08-28: Plan-label grounding lists EVERY listing room with dims in one sentence (unlike the single-room staging sentence) since a plan spans the storey; stored in the same jobs.grounding_used field.
- 2026-08-28: Tour viewer tears down and rebuilds all Marzipano scenes whenever scene data changes (no incremental diffing) — single-user tool, panos are browser-cached, and it keeps hotspot DOM in sync for free.
- 2026-08-28: Tour editor state is remounted via a React key of tour.id + scene-id set — server-confirmed scene changes (upload/save) re-seed local edits instead of merging; unsaved renames are lost on upload, acceptable for one user.
- 2026-08-28: Hotspot placement = stationary click (<5px pointer travel) in the viewer, distinguishing it from Marzipano's drag-to-pan; removing a scene client-side also strips hotspots targeting it so saves never point at deleted scenes.
- 2026-08-28: Copywriting reuses INTERPRETER_MODEL (Haiku 4.5, vision-capable) and interpreterCostCents — cheapest adequate per CLAUDE.md; if copy quality disappoints, swap the model id in config/models.ts, nothing else changes.
- 2026-08-28: listing_copy is one row per (listing, tone) — regenerating a tone overwrites only that tone; edits persist per tone; facts jsonb rides the row so the form re-seeds.
- 2026-08-28: spend_ledger kind check extended with 'copywriting' in 0005 (edit_type='COPYWRITING' set for phase-15 aggregation) rather than overloading kind=interpreter.
- 2026-08-28: Migration 0005 could NOT be applied live this session — no Supabase MCP connected and the permission classifier blocked keychain reads (CLI token) twice; left for Matt's SQL editor or an MCP session, per the blocker note in PROGRESS.md.
- 2026-08-28: ~100/250 word targets are prompt-enforced only (live word counts shown in the UI, no server validation) — MLS tolerances are loose and the user edits in-app anyway.
- 2026-08-29: HDR fusion = single-scale well-exposedness weighting (Mertens-lite, no contrast/saturation terms, no pyramid blending) — upgrade to multiband only if halos on high-contrast window edges annoy; pure code, so no spend_ledger row.
- 2026-08-29: HDR merged photo is a normal photos row in originals; the optional IMAGE_ENHANCEMENT chain is a client-side /api/jobs call on the returned photoId — no special job kind, no route coupling.
- 2026-08-29: react-konva pinned to 19.0.7 — 19.2.x requires react ^19.2.0, project is on 19.1.0; bump both together when upgrading React.
- 2026-08-29: Aerial annotation is fully client-side (ops-list undo, shapes derived by folding ops); export = Konva toDataURL at pixelRatio 1/scale for original-resolution PNG, and "Save to listing" reuses /api/upload verbatim — no new storage path or table.
- 2026-08-29: AERIAL_EDITING shares IMAGE_ENHANCEMENT's option shape (sky/grass) so the job panel reuses one option form; PORTRAIT_RETOUCHING has zero options — the conservatism is the template, and it skips auto-QA (the QA prompt judges listing-photo geometry, not faces).
- 2026-08-29: MTD spend aggregates via the admin client — the ledger RLS policy scopes through job_id, which hides pre-job interpreter rows (job_id null); the dashboard must count every call.
- 2026-08-29: BoxBrownie comparison prices live in config/models.ts (BOXBROWNIE_CENTS + default fallback, approximate list prices, data not code); estimate = Σ BB price per non-REWORK step over COMPLETED file groups only (failed runs delivered nothing; BB reworks are free).
- 2026-08-29: Ledger spend rows with null edit_type are labeled by kind (interpreter/qa/…) in the MTD breakdown — no schema change, every cent lands in exactly one row.
- 2026-08-29: Darkroom visual identity (spec artifact c59ca593…, decided in a separate brand session) scheduled as phase 18, AFTER deploy — infrastructure first, re-skin redeploys cheaply; ordered before video reels so new UI is built in the new system. Matt: "we need to add the visual UI elements to a phase as well."
- 2026-08-29: Listing video reels scheduled as phase 19, two tiers: Tier A Ken Burns slideshow (ffmpeg/Remotion, pure code, no ledger — HDR_MERGE precedent), Tier B optional fal image-to-video per photo (~$0.30–0.35/clip) on the existing state machine. Prompted by Matt's AutoReel research request.
- 2026-08-29 (research, not yet built): "one residential-interior model handles exposure, WB, lens correction, highlight recovery" is the wrong architecture and the wrong sales claim. Three of those four are deterministic and solved — a diffusion model can only approximate them, and approximating lens/perspective correction is exactly how GEOMETRY_INTERIOR gets violated. Findings: (a) LENS — EXIF gives camera+lens, Lensfun holds the distortion/TCA/vignetting polynomials for that exact glass (same DB darktable uses); ~50ms, $0, and nameable in copy. (b) WHITE BALANCE — the real-estate hard case is *mixed illuminant* (tungsten lamps + daylight window in one frame), which no global multiplier fixes; solved by Afifi/Brubaker/Brown WACV 2022 (render 5 WB presets, small net predicts per-pixel blend weights, github.com/mahmoudnafifi/mixedillWB). (c) EXPOSURE — global via sharp/libvips linear-space gain + tone curve; spatially non-uniform via Retinexformer or HVI-CIDNet (CVPR'25, NTIRE'25 winner, open weights). (d) HIGHLIGHT RECOVERY — from a single clipped JPEG this is invention, not recovery; only honest from RAW or from a bracket set, which lib/hdr.ts already does. Architecture: deterministic "develop" stage in front of the generative stage (hdr.ts is the precedent — pure code, no ledger row), and only pay fal when a generative option is actually on (sky, grass, TV/fireplace, reflections, staging, declutter). Enhancement-only jobs drop from ~$0.021 + 5-8s to ~$0 + sub-second.
- 2026-08-29 (research): no purpose-built real-estate enhancement LoRA exists publicly — only interior *design* LoRAs (SedatAl/Interior-Flux-Lora) and generic realism/detail ones (flymy-ai/qwen-image-realism-lora, Civitai "Real Life LoRA Qwen"). Training our own is cheap and license-clean: fal's Qwen-Image-Edit trainer is $4/1000 steps ($0.40 minimum at 100), Qwen-Image-Edit is Apache 2.0 so weights and adapter are commercially ours, and CLAUDE.md already bars the FLUX dev route. Dataset = Matt's own BoxBrownie original→edited pairs (he owns them); 50-200 pairs is the normal style-LoRA range. Cost caveat: fal's LoRA endpoint bills $0.035/MP vs $0.021 flat for base qwen-image-edit — the LoRA buys quality/consistency, NOT savings. Savings come from the deterministic pre-pass.
- 2026-08-29 (research): do NOT self-host yet — LOCAL_ENDPOINT stays a stub. fal serverless GPU is ~$0.0005/s (H100), so a 6s generation is ~$0.003 of raw GPU against $0.021 billed (~7x). Break-even on a dedicated H100 is ~90 img/hr at the $1.89/hr discounted rate, ~190/hr at $3.99 list — far above single-user bursty volume, and idle/cold-start makes it worse.
- 2026-08-29: Analyzed competitor restudio.ai (Dubai/UAE enhancement SaaS: enhance/stage/object-removal/2D→3D floorplans/descriptions, $0.09–0.20 per photo across tiers, 2K output cap, contradictory "we never fabricate" vs advertised sky swaps). Listing Studio already covers their entire catalog and more at 3–5x below their price floor. Only feature adopted: enhancement style presets (4 named looks as prompts.ts variants), added to phase 18 as a ride-along. RAW/TIFF input support considered and skipped — Matt's workflow doesn't produce RAW; revisit only if it does. Full teardown in project memory `restudio-competitor-analysis`.
- 2026-08-29: Matt: "I dont want to do a lora or training for now." LoRA training is OFF the table — do not re-propose it. The 2026-08-29 research entries above stand as reference only.
- 2026-08-29: Lens correction — Lensfun/database-driven correction NOT adopted. Real finding is smaller: IMAGE_ENHANCEMENT's prompt contains a self-contradiction ("straighten verticals, and correct lens distortion" vs GEOMETRY_INTERIOR's "do not alter ... camera perspective") and nothing in the codebase reads EXIF or applies orientation (no `.rotate()`/autoOrient anywhere; upload stores original bytes and only calls sharp().metadata()). Fix the contradiction and the orientation before considering any lens-profile work; a single-camera user needs one set of coefficients, not a database.
- 2026-08-29 (research — lighting/shadow control, Matt: "this is key"): Relighting IS a solved specialist problem with released weights, but the useful move needs no training and no self-hosting. Landscape: curated list at github.com/tandaily/Awesome-Relighting. **IC-Light** (ICLR 2025) — V1 commercially licensed, V2 (Flux-based) non-commercial upstream BUT fal hosts `fal-ai/iclight-v2` with a "Commercial use" badge at $0.10/MP; verify that badge with fal before it becomes load-bearing (same risk category as the FLUX-dev rule in CLAUDE.md). **LumiNet** (CVPR 2025, github.com/xyxingx/LumiNet) — indoor scene relighting by transferring lighting from a REFERENCE image, weights on HF, >20% over prior SOTA on MIT Multi-Illumination. **ScribbleLight** (CVPR 2025, github.com/chedgekorea/scriblit) — indoor relighting from scribbles, albedo-conditioned + ControlNet on normal maps, so it preserves colour/texture/geometry by construction; best fit for our GEOMETRY_INTERIOR constraint but needs a local pipeline. **RGB↔X** (SIGGRAPH 2024) — intrinsic decomposition (albedo/normal/roughness/irradiance), the physically correct route. Skip Neural Gaffer + DiLightNet: object-focused and the literature reports inaccurate shadows/reflections.
- 2026-08-29: On "preset camera settings" — the architectural prompt packs in circulation (ArchiGPT, Midjourney architecture collections, PromeAI lighting cheatsheets) are written for TEXT-TO-IMAGE render generation, not for editing a real photograph. In an edit model the camera already happened: lens/aperture tokens ("24mm", "f/8", "tilt-shift") have little purchase because geometry is pinned by the input image, while lighting DESCRIPTION (direction, quality, colour temperature, time of day) and material description do. Do not bulk-import render prompt packs into prompts.ts. Our own gap is that TURN_ON_LIGHTS and DAY_TO_DUSK carry thin lighting language next to rich geometry language.
- 2026-08-29: Structural conditioning noted for later — Qwen-Image ControlNet Union (InstantX) supports depth/canny/softedge/pose, and depth-conditioning pins walls and perspective far harder than a prompt sentence ever will (the real mechanical fix for geometry drift). BUT fal's hosted qwen-image-edit endpoint takes no control image, so this needs the LOCAL_ENDPOINT stub wired. Parked, not scheduled.
- 2026-08-29 (EXPERIMENT RUN, 4 fal calls, $0.138): tested whether a well-lit reference photo can substitute for a relighting model, before paying for IC-Light. Target = photo 93bb2736 (dim grey living room, fixtures off); reference = aadb20b0 (warm dusk living room, lamps glowing). Four arms, TURN_ON_LIGHTS verbatim: **A** qwen no-ref (today's shipped path) — 38.3s, lights essentially did NOT come on, room still flat and grey. **B** gemini + ref — lighting transferred convincingly BUT invented six wall sconces and a pendant cluster, and returned portrait. **C** gemini no-ref — 9.1s, geometry and framing preserved, genuine warm light, but invented a brass chandelier and two sconces. **D** gemini + ref + hardened prompt ("second image is a lighting sample only… add no new fixtures… preserve the first image's aspect ratio") — fixture invention fully suppressed, but still portrait and most of the lighting effect lost. VERDICT: reference-photo relighting is a NO on this stack; do not build it.
- 2026-08-29 **LIVE BUG (FIXED 2026-08-29, commit below)**: gemini's edit endpoint takes the OUTPUT ASPECT RATIO FROM THE REFERENCE IMAGE, not the primary. Measured: primary 1600x1000 + ref 1600x2400 → both ref arms returned 832x1248 (2:3, the ref's shape); both no-ref arms returned 1280x800 (16:10, the primary's shape). Explicit "preserve the first image's exact framing, crop and aspect ratio" did NOT fix it. Since `pickProvider(chain, hasRefs)` routes ANY ref-carrying FileGroup to gemini, every Sample Library / "by image" / "by URL" reference whose aspect ratio differs from the primary photo silently reshapes the output. Affects phase 11 inspiration features. FIXED: `padRefsToPrimaryAspect` in lib/imaging.ts — when provider=gemini with refs, each aspect-mismatched ref is letterboxed (sharp fit:contain, gray) onto a canvas at the primary's aspect (long edge 1536 — refs are style refs, resolution is free) and sent as a data URI, no storage round trip. Letterbox chosen over crop-the-output-back: gemini recomposes the whole scene at the ref's aspect, so cropping the output would slice room content. Live-verified (scripts/test-ref-aspect.mjs, 1 sync gemini call): same primary 1600x1000 + same 1600x2400 ref, padded → output 1280x800 (aspect 1.600, the primary's) vs the bug's 832x1248. Also proves the fal gemini endpoint accepts data URIs.
- 2026-08-29 **PROMPT BUG (FIXED 2026-08-29)**: TURN_ON_LIGHTS enumerates "ceiling lights, chandeliers, pendant lights, lamps, and sconces" and the model reads it as a shopping list to ADD — arm C invented a large brass chandelier and two wall sconces in a room that had neither. Arm D's clause ("Turn on only the light fixtures that already exist in the first image, and add no new fixtures of any kind") suppressed it completely. FIXED: TURN_ON_LIGHTS in lib/prompts.ts now uses the proven clause ("Turn on only the light fixtures that already exist in the image, and add no new fixtures of any kind; every existing fixture emits a warm inviting glow.") in place of the fixture enumeration.
- 2026-08-29: measured latency, against the "five to eight seconds per image" marketing line — qwen-image-edit 38.3s, gemini-2.5-flash-image ~9-10s (fal.run sync, includes queue). Both no-ref arms also downscaled 1600x1000 → 1280x800, so every generation loses resolution before the optional upscale step.
- 2026-08-29: All prod-deploy commands (`vercel deploy --prod`, `vercel redeploy`) are classifier-blocked for Claude — deploys are a Matt-runs-one-command step, like the env push; freshly generated secrets (openssl rand) CAN be piped to `vercel env add`, only .env.local reads are blocked.
- 2026-08-29: fal webhook JWKS lives at rest.alpha.fal.ai/.well-known/jwks.json (the tooling/public-keys path 404s) and fal pads its base64url jwk x values — strip trailing `=` before Node JWK import. Found in prod: every webhook 401'd until fixed; reconcile rescued at t+3m25s, live-proving the safety net + no-double-charge.
- 2026-08-29: CRON_SECRET is prod-only (generated, never in .env.local) — locally the reconcile route stays open by design (manual ?all=1 completion path); Vercel auto-sends it as the cron Bearer header.
- 2026-08-30: Reviewed Matt's market landscape doc (Downloads/md (1).md, Perplexity-style). Its RE Studio row ($2.58/room, 24hr, compliance checker, reels) contradicts the direct 2026-08-29 site teardown ($0.09–0.20/photo, 5–8s, neither feature advertised) — sourced from a third-party blog; the direct teardown wins. Sources 3–10 are competitors' own "BoxBrownie alternative" SEO pages; prices roughly credible, qualitative claims are marketing. Its "your existing MLS/RESO work" line looks like fabricated personalization — unconfirmed. Structural takeaways: staging/enhancement fully commoditized ($0.30–$1/image AI-native vs $16–30 human); differentiation = workflow bundling (compliance, video, tours, API) — which phases 14/19 + the interpreter loop already cover. One adoption: MLS compliance checker added as phase 21 candidate (extends the existing auto-QA vision pass; cheapest defensible differentiator). If the product ever sells: cost ≈ $0.05–0.10 per finished output (2.5 gens/FileGroup), market clears $0.30–$2.67 → price middle-quadrant, lead with chat-rework + compliance + reels, never per-image price.
- 2026-08-29: 360 edits are prefix-wrapped base templates, not new prose — `EDIT_360_BASE` maps 360_X → X and compilePrompt recurses then appends the `EQUIRECT_360` block (seam continuity, pole stretching, no aspect change). Keeps the brightness cue in the first ten words and the geometry sentences verbatim for free.
- 2026-08-29: 360 full-res path = qwen `image_size` 2048×1024 at submit (new optional `extraInput` on submitGeneration) + sharp lanczos resize of the result back to the source pano's exact width×height in completeStep (fit fill — the equirect must wrap the sphere at original dims). ponytail: lanczos upscale, not Real-ESRGAN; add the fal upscale call only if pano sharpness disappoints.
- 2026-08-29: 360 chains force the qwen provider and reject sample refs — refs force gemini, whose output aspect follows the REFERENCE image (live bug, 2026-08-29 entry above), which would wreck a 2:1 equirect. Mixing 360 and flat edits in one chain is rejected; input must be a non-floor-plan photo with stored width/height within 2:1 ±0.1 (same tolerance as tour scenes).
- 2026-08-29: 360 outputs skip auto-QA (the QA prompt judges flat listing-photo geometry, which equirect distortion legitimately "violates") and instead every version inserts with qa_note pre-set to the seam/pole manual-review note — the existing QA line in the UI doubles as the experimental review flag, no schema change.
- 2026-08-29: Interpreter deliberately does NOT know the 360 edit types (picker-only) — free-text chat on a pano would otherwise let Haiku pick flat edits for equirect inputs or vice versa; the sanitizers already reject unknown types so nothing needed changing.
- 2026-08-29: 360 preview reuses TourViewer with a single synthetic scene per output (width from photos.width, which PhotoRow now carries). Known cosmetic gap: the cost-estimate line uses pickProvider, so a hypothetical 3+-step 360 chain would display kontext pricing while actually running qwen — not worth a special case.
- 2026-08-29: Darkroom theme ships media-based (`prefers-color-scheme`), no in-app toggle — the class-based `@custom-variant dark` was removed, so Tailwind's default media `dark:` variant drives shadcn primitives and Matt's OS appearance setting is the light/dark switch (single user, zero toggle UI).
- 2026-08-29: Favicon = `app/icon.svg` (the 16px-tuned mark on a dark rounded rect; Next serves it natively) — `favicon.ico` deleted, no ico generation. The three hand-tuned brand SVGs live in `public/` as assets; the in-app mark is inline JSX (`components/brand.tsx`) with currentColor crop marks so it tracks the theme, teal room/floor fixed.
- 2026-08-29: Enhancement style presets are picker-only (like the 360 types) and ride the step options in edit_chain jsonb — "recorded on the job record" for free, no migration. `natural` is an empty clause: the base IMAGE_ENHANCEMENT template IS the natural look; the other three append one grading sentence each (one aesthetic per prompt).
- 2026-08-29: `font-sans` must be applied on the body rule in globals.css — next/font attaches its CSS variables to `<body>`, while Tailwind preflight sets the default font at `html` level where the vars don't exist yet; found live (body rendered system font until fixed).
- 2026-08-29: Watermark + plan-band text use `'JetBrains Mono','Courier New',monospace` in the sharp-rasterized SVGs — server rendering uses installed system fonts, so Courier is the honest fallback that keeps the mono look on Vercel; never web-font loading in lib code.
- 2026-08-29: Plan-band "never teal" pixel check asserts distance-to-brand-teals < 50 px count, not zero colored pixels — subpixel text antialiasing produces red/cyan glyph fringes (even the old Helvetica band had ~2000), while any drawn teal element is hundreds of pixels. Check lives in scripts/check-brand-output.ts.
- 2026-08-30 (research — official prompting guides vs prompts.ts): Reviewed prompts.ts against the official/primary prompting guidance for all three providers (Qwen-Image-Edit prompt guides incl. HF 2511 discussion + deapi guide; Google's Gemini image-editing doc templates; BFL Kontext i2i guide). Template architecture VALIDATED: imperative operation + named target + specification + preservation clause is exactly Qwen's recommended four-part formula, and explicit end-of-prompt preservation ("keep all other elements unchanged") is every guide's #1 rule — we already do both. Gaps worth acting on, in value order: (1) **One operation per prompt** is Qwen's strongest rule and IMAGE_ENHANCEMENT stacks ~6 ops in one qwen call (WB+sharpen+dust+window exposure+TV+fireplace) — consistent with arm-A underperformance in the 2026-08-29 relight experiment; the already-logged deterministic develop stage is the fix (fewer AI ops per prompt), not 6x chaining. (2) **negative_prompt is unused** — fal's qwen endpoint accepts it; guides recommend 3–6 terms per failure mode (staging: "warped walls, distorted perspective, floating furniture"; floor plans: "misspelled letters, garbled text"). Cheap, additive, per-template. (3) **Preservation-first ordering for complex edits** — Qwen guide: anchor what must NOT change before describing the change; our geometry sentence rides near the end after style/options clauses. Conflicts with the brightness-cue-first rule only slightly (cue = words 1–10, preservation can be sentence 2). (4) **Provider-aware compilation is the actual moat**: one compilePrompt feeds three models with different dialects (qwen: terse imperative+negatives; gemini: natural-language scene description, official template "change only X, keep everything else exactly the same, preserving original style/lighting/composition"; kontext: direct naming, "while maintaining the same..." clauses, no pronouns). Same semantic spec, per-provider rendering. (5) LISTING_SUFFIX + "wide-angle" is t2i-style keyword tail with little purchase on edit models (same finding as the 2026-08-29 camera-settings entry) — test-removal candidate on qwen, but CLAUDE.md rule 4 mandates it, so measure before touching. Comment normalization: user free text appends raw; descriptive phrasing ("cozy vibes") is Qwen failure mode #1 — the interpreter should emit an imperative-normalized comment alongside the preserved original.
- 2026-08-30 (feature candidate — markup-to-edit): Matt asked for click/drag/circle annotation to drive edits ("remove the blue circle, change the red square to a recliner"). Externally validated: Google shipped exactly this ("draw-to-edit"/Sketch markup) in the Gemini app Dec 2025 on this same model family — gemini-2.5-flash-image natively follows colored markings on the input image and omits them from output. Build shape: reuse the phase-14 Konva annotation canvas on the FileGroup composer; flatten annotated PNG as the MODEL INPUT (clean original stays the stored source); interpreter maps each mark color+shape to an instruction; prompt clause: "The colored markings on the image are editing instructions only — [blue circle: remove the marked item; red rectangle: replace the marked item with a leather recliner]. Do not render any markings in the output." Route markup jobs to gemini (markup-following is its signature; marks are on the PRIMARY so the ref-aspect bug doesn't apply). Needs ONE live experiment before scheduling: verify marks don't leak into qwen output (if qwen fails, gemini-only is fine). Phase candidate alongside the phase-21 compliance checker; not scheduled.
- 2026-08-30 (phase 19 input): aicameramovements.com reviewed — 46 reusable camera-movement prompt terms for AI VIDEO models (Kling/Seedance/Runway etc.), each prompt separating camera instruction from scene content. Zero value for still-photo editing (camera tokens don't bite on edit models — see the 2026-08-29 camera-settings entry), but directly applicable to phase 19 reels Tier B (fal image-to-video per photo). Adopt as a small curated preset list, not the full 46 — listing-appropriate moves only: interiors "slow dolly in", "slow zoom in", "pan right/left", "slow zoom out" (reveal); exteriors "drone push in", "drone pull back", "orbit clockwise", "crane up". Pattern to copy: camera term + scene stays constant, so the per-photo prompt is just preset + one scene line.
- 2026-08-30 (phase 19): Tier A reels ship pure-code per plan; decisions made in-phase: (a) background render = next/server `after()` post-response kick + reconcile-cron rescue (fail rendering >15 min, render orphaned queued rows) — no Inngest/queue infra, claim-conditional transitions make duplicate kicks no-ops, state fully in the reels table so a dev-server restart loses nothing; (b) ffmpeg binary via ffmpeg-static npm (serverExternalPackages + outputFileTracingIncludes carry the binary into Vercel functions), FFMPEG_PATH env overrides, PATH last resort; (c) NO music tracks bundled — can't fabricate royalty-free audio; picker lists `assets/music/*.mp3` at page render (dir ships with README pointing at Pixabay/Incompetech/YT Audio Library), "No music" default, music path in the renderer is code-complete but untested until a track exists; (d) caption facts pulled from listing_copy.facts (phase 13) when present, address-only otherwise — no new facts form; (e) zoompan jitter mitigated by 2x supersampled stills, 4 alternating camera moves hardcoded (aicameramovements presets are Tier B input, not Tier A); (f) Tier B (fal image-to-video) deferred per plan — revisit only if Ken Burns motion feels flat after Matt's eyeball test.
- 2026-08-30 (phase 19): migration 0006 (reels) written but NOT applied — no Supabase MCP in this session and DDL is Matt-only (DECISIONS 2026-08-29 access-paths precedent). Phase marked complete with the blocker flagged, same as phase 13's 0005.
- 2026-08-30 (phase 20): Terms jurisdiction swapped Dubai/UAE → State of Florida, United States (Matt is Florida-based; not counsel-reviewed — Matt's call per PLAN). The source doc's DIFC "this section must be replaced if…" meta-sentence dropped with the swap. Operator swapped ReStudio/ReStudio.ai → Listing Studio. lib/terms.ts is GENERATED — transform (node, throws if any swap misses): strip preamble before first `---` and acceptance-note after last `---`, apply the two jurisdiction sentence replacements + DIFC-sentence removal, then replaceAll ReStudio.ai→Listing Studio, ReStudio→Listing Studio, emit `export const TERMS_TEXT = JSON.stringify(text)`. Edit docs/terms-of-use.md and re-run if the source changes.
- 2026-08-30 (phase 20): terms_acceptances = one row per (user, version), PK composite — history kept as acceptance evidence (terms §27), TERMS_VERSION bump naturally re-prompts (no row for the new version). No profiles table exists and none was invented; auth.users FK directly.
- 2026-08-30 (phase 20): Gate check lives in the root layout (server) — one RLS select per render, single-user cheap; TermsGate client component returns null on /terms and /login so the terms stay readable pre-acceptance. Pre-migration (0007 unapplied) the layout's acceptance query errors and the gate FAILS OPEN — deploy is safe before the SQL editor step, same pattern as reels/0006.
- 2026-08-30 (phase 21): Proceeded on the "candidate" phase without a fresh confirm — /resume is the go signal, the checker is flag-only/additive (cheap to reverse), per the ambiguity rule.
- 2026-08-30 (phase 21): Compliance = the SAME auto-QA vision call in a compliance mode (COMPLIANCE_QA_SYSTEM extends QA_SYSTEM with a `checks` array; check ids listed in the request) — one ledgered qa call, never a second pass; dusk's two named checks folded in as check ids, replacing the DUSK_QA_CHECKS request appendix (constant kept, now unused).
- 2026-08-30 (phase 21): "Virtually Staged" label check is metadata, not vision — seeded pass ("label defaults ON at download") on version insert for staged chains; a label-off download flips it to fail STICKY (an unlabeled copy is already out, so a later labeled download never un-flags). Zip route untouched — it always applies the staged-default watermark, no fail path.
- 2026-08-30 (phase 21): compliance jsonb read via a separate fail-open query on the listing page — putting the column in the nested jobs select would error the WHOLE query pre-migration-0008 and brick the page; orchestrator's version update likewise retries qa_note-only when the compliance column is missing. Migration 0008 pending in Matt's SQL editor alongside 0006/0007.
- 2026-08-30 (phase 21): Phase-6 manual dusk checkboxes render only when a version has no compliance checks — the automated checklist supersedes them (their DECISIONS entry always called them a stopgap).
- 2026-08-30: Local build = `npm run build` (next build --turbopack). Bare `npx next build` (webpack) FAILS on react-konva→canvas resolution — not a regression, just the wrong bundler; don't chase it.
- 2026-08-30 (phase 20): No markdown dep — the terms doc uses exactly three block shapes (## heading, numbered-list run, paragraph), so terms-body.tsx parses with a 15-line splitter; any unrecognized block degrades to a paragraph. Radix Dialog (radix-ui already installed) over a bare div for the focus trap + aria; non-dismissable by preventDefault on escape/outside/interact-outside and no close affordance rendered.
- 2026-08-30 (phase 23): Proceeded on the "candidate" phase without a fresh confirm — /resume is the go signal, phase 23 precedes 24 in PLAN order, and its gate is a 6¢ experiment (cheap to reverse), per the ambiguity rule + the phase-21 precedent.
- 2026-08-30 (phase 23 gate, scripts/test-markup.mjs kept as regression): blue-circle + red-rect marked living-room photo → one sync call each. NEITHER provider leaks marks (0 mark-colored px in both outputs vs ~13k/11k in the input; pixel thresholds in the script). But qwen only half-obeys: replaced the boxed coffee table, IGNORED the circled lamp. Gemini did both. So MARKUP_EDIT forces gemini at the jobs route — exactly the fallback the 2026-08-30 candidate entry predicted.
- 2026-08-30 (phase 23): Mark semantics fixed by tool/color — blue circle = remove, red rectangle = replace — no per-mark labeling UI; the fg comment describes replacements (template says "as the client notes describe"; comment-less replaces fall back to "similar item in better condition, style-matched"). Interpreter deliberately unaware of MARKUP_EDIT (picker-only, same stance as the 360 types) — the fixed color semantics make the "interpreter maps marks" idea from the candidate entry unnecessary.
- 2026-08-30 (phase 23): Flattened markup lives in originals at `{user}/markup/{uuid}.jpg` (jpeg q92 — saturated mark colors survive, gate-proven), NOT a photos row; referenced from the MARKUP_EDIT step's options.markup_path (jsonb — no migration), served to the model by an inputUrlForStep branch. Clean original stays the stored source; runs-alone + single-photo + own-prefix validation at the jobs route.
- 2026-08-30 (phase 23): MARKUP_EDIT does NOT count as staged for the watermark default (isStaged untouched) — a replace does fabricate an item, so if Matt wants label-default-ON for markup replaces, add MARKUP_EDIT to STAGED types in lib/deliver.ts + job-panel; one-line change, flagged not made.
- 2026-08-30 (phase 23): Konva drag-draw keeps the in-progress mark in a ref, state only for rendering — mousemove state updates flush at continuous-event priority (mouseup's closure saw a stale zero-size draft), and committing inside a setDraft updater double-fires under StrictMode (duplicated every mark in dev). Both bugs hit live in the browser test; ref pattern fixes both.
- 2026-08-29 (phase 24): Provider dialects are a POST-PASS on the compiled base prompt, not per-template rewrites — the existing templates are the qwen dialect (live-proven; negatives via compileNegative already carry qwen's failure-mode shaping). A terser qwen rewrite or the preservation-first ordering A/B (geometry as sentence 2) would change proven prompts on subjective quality grounds, so both are DEFERRED until measured with Matt's eyeballs + spend; LISTING_SUFFIX stands per CLAUDE.md rule 4. Dialect clauses insert BEFORE the suffix so rule 4 holds in every dialect. FLOOR_PLAN_REDRAW is dialect-exempt (a redraw transforms the whole image — "keep everything else the same" would fight it).
- 2026-08-29 (phase 24): comment_imperative stored WITHOUT a migration — it rides fg.comment (the field compilation already reads) while the user's verbatim words stay on the record via the job title and chat_messages (job cards already display the latest user chat message). A dedicated column would need SQL-editor access Matt gates; add one only if the verbatim comment ever needs to live on file_groups itself.
- 2026-08-30 (post-phase-18 polish, not a PLAN phase): Motion layer appended to app/globals.css — four CSS-only primitives, no library (framer-motion et al deliberately skipped; tw-animate-css already present covers nothing product-specific). `develop-in` fires on mount via @starting-style (no effect hook / mount flag) and runs brightness 1.55→1 + contrast 0.5→1 + saturate 0.35→1, i.e. the actual curve of a print surfacing in developer, not a generic fade. `pulse-live` is gated to running/processing ONLY — a pulsing "complete" would break the same discipline the teal follows. `sweep` tints from color-mix(in oklch, var(--state-running) …) so it derives from the token instead of hardcoding teal; failed frames get no sweep (nothing is happening, nothing should move). `stagger` is transition-delay on nth-child so it composes with develop-in rather than duplicating it. All four disabled under prefers-reduced-motion.
- 2026-08-30: Verified the motion layer with `npx @tailwindcss/cli -i app/globals.css -o <scratch>` rather than `npm run build` — a build would have killed another session's dev server on port 3000 (see the never-build-while-dev-runs rule). Confirms Tailwind v4 does NOT tree-shake these plain `@layer utilities` rules and emits @starting-style intact. Use this trick for any CSS-only change while a dev server is live.
- 2026-08-30: Port 3000 left alone rather than setting autoPort — this app's Supabase magic-link callbacks and fal webhooks are pinned to localhost:3000, so the motion layer is NOT yet verified in the running app. Visual check pending next time a server is free.
- 2026-08-31 **UI REDESIGN DIRECTION LOCKED (Matt: "lock this in"; not yet built — needs PLAN phases before any code).** Mockup of record: https://claude.ai/code/artifact/c302dc51-2259-486c-8e4c-b17a52e3870f (3 artboards: listing page, FileGroup detail, Tools subnav). Two halves, both approved:
  (a) **Structure** (from a two-pass agent analysis, second pass adversarial): one shared photo tray replaces the FIVE separate photo pickers on the listing page (PhotoGrid + JobPanel + AerialPanel + ReelPanel + TourPanel each hold their own selection state today); ONE composer where chat text materializes into editable chain steps WITH a cost estimate before Run (today chat fires /api/jobs blind — simulateCents only renders for the manual builder; this merge kills both the blind spend and the dual-picker confusion); job feed stays unfiltered (selection clears on run, so a selection-filtered feed would hide the job just launched); each FileGroup gets its own route /listings/[id]/f/[fileGroupId] (versions, rework thread, QA, compliance, downloads — today buried in an inline accordion); specialty tools (aerial/reel/tour/plan) move to /listings/[id]/<tool> behind a subnav. Selection arity is per-tool (chat/markup=1, batch=N, reel=ordered N) — shared tray, not one universal selection rule. Power-user accelerators worth building: apply-last-chain-to-selection, per-listing default chain, range-select, Enter-to-run. Sequencing: cheap pass first (kill duplicate pickers, move tools out, card-ize) ≈60% of value, then composer merge, then FileGroup route.
  (b) **Visual — "Editorial Luxury", supersedes Darkroom (phase 18) app-wide.** Target feel: luxury hotel / luxury furniture site (Matt's refs: RH + West Elm "style and quality of the images, the rooms" — the imagery bar, not their literal sites). Light warm ground, brass accent, serif display: --bg:#F3EEE4, --card:#FDFBF7, --popover:#EDE4D5, --fg:#241F1A, --muted-fg:#83766A, --border:#DED0BC, --input:#CBB994, --brass:#A57C3F, --brass-fg:#FBF6EC, --accent:#EDE1C8, --accent-fg:#7A5A2A; states --complete:#5C7A52 --failed:#A8483F --qa:#B8842A --queued:#83766A. Type: Cormorant Garamond for hero/display (address, job titles), DM Sans for UI labels/pills/uppercase-tracked micro-text, Public Sans body. JetBrains Mono DROPPED from UI (research: reads "developer tool" to the 57-median-age realtor audience; dark mode likewise absent from every realtor-facing comparable checked — BoxBrownie, Follow Up Boss, kvCORE, PhotoUp, Matterport, Compass). Radius 2-3px squared, hairline borders, state pills = colored dot + tracked uppercase label (no tinted fill), full-bleed photo hero with dark scrim + serif address on the listing page, NO per-edit dollar figures anywhere in UI (pricing stripped from mockup on Matt's ask — cost tracking stays in the ledger/dashboard, not the editing surface). Brand mark/wordmark treatment TBD in-build (Darkroom's mono wordmark now clashes with the new type system — flag, don't improvise).
  Dark mode: the redesign is light-only by design (inverse of Darkroom's zero-toggle rule); dark `prefers-color-scheme` blocks in globals.css get replaced, not maintained. Photography quality is load-bearing: mockup used Higgsfield soul_location generations (RH-catalog-style prompts; "no people" alone insufficient — needs "empty, no people or figures in frame, crisp modern digital photography, no film grain").
- 2026-08-31 (post-phase-25): Matt's reaction to the token swap, verbatim: "we didnt do aynthing witht eh flow, the process etc. DId we? also remove the prices. WE were doing a full redsign of the flow, the homepage, etc no?" Three rulings: (1) **prices come off EVERY UI surface including the dashboard** — the redesign-lock entry's "dashboard keeps $ spend" carve-out is dead; spend_ledger keeps recording (CLAUDE.md invariant), it just has no UI for now; (2) **homepage redesign added as phase 26** (luxury front door: photo-card listings w/ derivable cover photo, serif greeting, no cost report) — the arc's later phases renumbered 27–32; (3) sequencing lesson: leading with a reskin phase read as "nothing changed" — when resuming, frame each phase by the flow change it delivers.
- 2026-08-31 (phase 33): IA split to public `/` → `/login` → `/dashboard`. The dashboard MOVED to `app/dashboard/page.tsx` (with `dashboard-live.tsx` alongside it — only page.tsx imported it, so the `./dashboard-live` relative import survives the move); `app/page.tsx` is now a public, auth-free landing that only calls `getUser()` to flip its CTA (never force-redirects — the landing stays viewable when signed in). Middleware allows a `publicPaths=["/","/login"]` set unauthed instead of `/login`-only; authed-on-`/login` now targets `/dashboard`. No matcher change needed (the guard covers `/`). TermsGate gained `/` to its null-return paths so it only gates authed app surfaces (now under `/dashboard`). Link sweep: login post-auth `push("/")` and the listings "Dashboard" nav `href="/"` both → `/dashboard`; the Wordmark is a plain span, not a link, so no lockup change. Light-only, no prices — consistent with phase 26.
- 2026-08-31 (phase 27): Tool panels physically MOVED into route folders (`git mv aerial/reel/tour/plan-panel.tsx` under their `app/listings/[id]/<tool>/` dirs) rather than left in place and imported from `../` — the only relative imports to rekey were two type-only `./photo-grid` → `../photo-grid` (tour/reel panels use `@/` aliases + `@/components/tour-viewer`), so co-location cost ~nothing. Floor plans (grid + PlanPanel) moved WHOLESALE to `/plan` to genuinely slim the listing page to upload/photos/rooms/jobs; floor-plan UPLOAD stays on the listing page via the shared UploadPanel (splitting it is phase-29+ tray work), so `/plan` shows a where-to-upload hint when empty and PlanPanel self-returns null with zero plans. `floorPlans` is still derived on the listing page and passed to JobPanel (plan-fg before-image lookup needs it). ToolsNav is a client component (usePathname, exact-match active) rendered on the listing page + all five tool routes; the old header "Listing copy →" link is gone (Copy lives in the subnav now). Build NOT run locally (dev server on :3000 — never-build-while-dev rule); tsc + eslint clean, all six routes verified live incl. reel realtime + mobile two-line wrap. Landing next/image → plain <img> from the imagery pass is unrelated to this phase.
- 2026-08-31: Phase 28 FileGroup workspace realtime = narrow `postgres_changes` eq filters (file_groups on id, output_versions + chat_messages on file_group_id) rather than the unfiltered-3-table fallback — file_groups/output_versions are both in the realtime publication and are the load-bearing signals (a rework's new version + step_status change both fire on the fg's own rows), so the fallback wasn't needed; chat_messages rides the same rework's output-version insert even if it's not published.
- 2026-08-31: Phase 28 fg page loads the parent job via `jobs!inner(...)` embed and 404s on both no-row (RLS) and `job.listing_id !== id` mismatch; only the primary photo is fetched for the before-image (the transplanted detail never looks up any other photo), not the whole listing's photo set.
- 2026-08-31: Phase 28 left job-panel.tsx at 995 lines, not the spec's ≈600–700 — the ~460-line composer form (all per-edit option UIs) is what remains and is untouched by this phase; its split into composer.tsx + job-feed.tsx (and job-panel's deletion) is explicitly phase 30's job, so no premature refactor here.
- 2026-08-31 (phase 31): "Apply last chain" is DERIVED from the newest non-ideas job's edit_chain at render (zero storage, per PLAN) — REWORK + MARKUP_EDIT steps are stripped (REWORK is internal-only, MARKUP_EDIT is arity-1 and bound to a one-off drawn markup image, so neither is a batchable repeat). "Listing default" is `localStorage['ls:defaultChain:<listingId>']` (single-user, no migration, no server round-trip); read once in a SSR-safe useEffect with try/catch so corrupt/blocked storage degrades to "no default" instead of crashing. applyChain deep-clones over EDIT_TYPES defaults so editing a materialized option never mutates the source job/default. New loose local type `ChainLike` (options optional) reconciles JobRow.edit_chain (options?) with ChainEdit (options required) — applyChain fills defaults, so accepting the loose shape is safe.
- 2026-08-31 (phase 31): Select polish converted the RECTANGULAR raw `<select>`s to the phase-25 `ui/select` with `className="w-auto"` (twMerge overrides the component's `w-full` to keep current auto width). Deliberately LEFT RAW three bespoke-compact selects — composer's 3 chip pills (rounded-full text-xs), photo-grid's over-photo room-tag, fg-workspace's text-xs py-0.5 download-variant — because ui/select's h-9/w-full would visually inflate them and the override noise (h-auto py-0.5 rounded-full …) defeats the drop-in point. No stray mono/dark remnants existed to sweep: globals.css tokens + brand.tsx's interim Mono Wordmark (TODO(brand)) are both intentional.
- 2026-08-31: Phase 30 composer materializes interpreter `ideas` results as **4 labeled read-only mini-chains** (label + chain-type summary) with one Run, not 4 independently-editable chain editors — the spec said "labeled mini-chains", and 4 parallel option-form editors would triple the composer's state surface for a feature that's about exploring directions, not fine-tuning them (refine happens after promotion, on the FileGroup route). `job` results DO materialize into the full editable `ChainStepEditor`. Interpreter clarifying questions can chain (style → which-room → …); each stays in draft with zero blind fire, so the "exactly one question" DoD phrasing means one-at-a-time, not one-total. Shared consts live in a plain `edit-types.ts` (no "use client") imported by all three components; the JobRow/SampleRow/ComplianceNote types moved to `job-feed.tsx` (the feed owns them) with the two f/[fileGroupId] importers re-pointed.
- 2026-08-31 (phase 32): Output-artifact brand pass — watermark pill + reel caption swapped Darkroom teal (#7FD9D4 / #3FBFB9) → Editorial-Luxury brass #A57C3F and mono → serif (`'Cormorant Garamond', Georgia, 'Times New Roman', serif` — sharp renders via fontconfig, so lead with the brand serif but keep real system fallbacks; Cormorant isn't system-installed so it lands on Georgia/mac or a Linux serif on Vercel, both fine). KEPT both dark scrims (watermark #0B0E10@82%, reel black@62%) even though the app palette is now light: the scrim sits over an arbitrary photo/video frame of any brightness, so a dark backing is a legibility requirement, not a palette leftover — PLAN only asked for the color/font swap, not a scrim change. Bumped the watermark text 13→15px because serif reads visually smaller/thinner than mono at the same px; still fits the fixed 246px pill (proportional serif is narrower than the old mono). `lib/plan.ts` untouched (address/disclaimer bands stay pure black-on-white — plans get printed/photocopied, per phase-18 DECISION). check-brand-output.ts watermark assertion retargeted teal→brass (warm r>g>b detector); check-reel.ts `colors` array is synthetic still-backgrounds, not a caption assertion, so its incidental #3FBFB9 was left as-is.
- 2026-08-31 (post-PLAN feature work, guided-QA session — NOT a phase; see PROGRESS.md "ACTIVE HANDOFF"): Floor-plan → rooms parser built as a VISION pass, not a redraw — transcribes only what the plan prints (CLAUDE.md "never infer plans from room photos" respected; this reads the plan itself), returns proposals for a human-review gate (never auto-creates rooms — vision isn't 100%), and writes the reviewed set through `createRooms`, one bulk insert + one revalidation. The initial reuse of `createRoom` 26 times caused the server room tree to change across successive action responses and produced a live hydration mismatch; bulk insertion fixes it while the manual one-room form keeps `createRoom`. Ledgered `kind:'interpreter'` (an allowed enum) with `edit_type:'FLOOR_PLAN_PARSE'` to AVOID a DDL migration for a new ledger kind.
- 2026-08-31: Floor-plan OCR uses `VISION_PARSE_MODEL = claude-sonnet-5`, NOT the Haiku `INTERPRETER_MODEL`. Haiku 4.5 dropped the second inch digit (read 13'11" as 13'1"); Sonnet 5 reads it correctly (headless probe confirmed on the Elam plan, 26 rooms). max_tokens 8192 — a busy plan (~26 rooms × name+type+4 dims+x,y) exceeds a smaller cap and truncates the JSON → "couldn't read that plan". Gemini 2.5 Pro is the documented fallback lever (best dense-text OCR; 1-line model swap) if Sonnet ever stumbles. Cohere is NOT for OCR (its vision = embeddings); Cohere Embed is reserved for the deferred photo→room matching, and even there start with Claude/Gemini vision compare (zero new deps).
- 2026-08-31: Room dimensions captured as feet + inches SEPARATELY (`length_ft/length_in/...`) at the parser + review UI (Matt: "do feet and inches, not .9 feet ... a box for each, feet box inches box"), but STORED as decimal feet (`length = ft + in/12`) because `rooms.length/width` is a single numeric — no schema change. Display formats decimal feet back to `13′9″` (`fmtDim`). Metric plans keep the value in `_ft` and pass through as `m`.
- 2026-08-31: Floor-plan 2D overview shows a draggable pin per room from model-returned normalized x,y (Matt: "edit names, dimensions right in the floor plan overview 2d"). Pin drag is COSMETIC/session-only — positions are NOT persisted (no rooms.x/y column) and don't affect created rooms; persisting them is a future migration. Only name/type/dims are written.
- 2026-08-31: `components/ui/select.tsx` root-cause fix — added `appearance-none` + a data-URI chevron so the macOS native bevel stops clashing with the flat brand buttons; padding trimmed to pr-6 so narrow tag dropdowns don't truncate the room name. This reverses part of the phase-31 "leave three bespoke-compact selects raw" call: the over-photo room-tag select is now `<Select>` (the appearance-none fix makes the drop-in look right at compact sizes).
- 2026-08-31: Full-screen single-photo editor added — clicking a grid photo opens the existing `<Composer>` scoped to one photo (Matt wanted a focused edit surface, not the inline strip). This supersedes the phase-29 "image-click toggles selection" interaction: image-click now OPENS; a corner ＋ badge is the batch-select affordance. Inline Composer still serves batch (2+). Dashboard gained an inline create-listing form (reuses createListing) so you don't have to detour to /listings.
- 2026-08-31 (guided QA): Elam Plan 5 is the floor-plan OCR acceptance sample — Sonnet 5 transcribed all 26 names and feet/inches exactly. Spatial pins are a separate failure: model-estimated normalized x/y values are not true OCR bounding boxes and several land visibly off-room. Do not infer OCR accuracy from pin accuracy or persist those coordinates as authoritative.
- 2026-08-31 (guided QA): Matt rejected the current Job-feed cards as "cheap" — nested bordered boxes, duplicate status, tiny preview, raw edit-type tokens, and the decimal grounding dump violate the Editorial Luxury direction. This is a locked redesign input, not an in-session polish tweak: append a PLAN phase before changing it; target an editorial activity feed with useful visual previews, one status, human title, technical details collapsed, and formatted dimensions.
- 2026-08-31 (phase 34): Rooms use ONE searchable selector and ONE shared photo tray, not a stacked card/accordion per room and not a second per-room gallery. `ListingWorkspace` owns the client-only active filter; changing it clears batch selection so hidden photos cannot remain queued. All/Untagged are virtual filter values, while duplicate generic room names are disambiguated for display only (ordinal labels plus dimensions)—stored names and schema remain untouched. One selected-room summary/editor replaces `room-panel.tsx`; add-room and floor-plan extraction remain adjacent. The separate upload-time quick-tag dropdown is removed as duplicate navigation; per-photo tagging remains in the visible tray.
- 2026-08-31 (guided QA diagnosis): the regular-photo upload failure is Next's request truncation, not Supabase: dev output logged `Request body exceeded 10MB for /api/upload`, followed by `req.formData()` failing because only the first 10 MB remained. Do not paper over it with a friendlier error alone. Plan a bounded larger body limit or direct-to-Supabase upload while preserving authenticated listing ownership, accepted media types, size bounds, metadata insertion, and the current room-tag contract.
- 2026-08-31 (guided QA): HDR merge is not an always-visible upload-toolbar action. Remove its button and enhancement checkbox from `upload-panel.tsx`; retain the pure-code route and fusion helper so a later conversational/contextual flow can invoke it without reimplementing the merge.
- 2026-08-31 (phase 35): The listing page is a PHOTO WORKSPACE, never an operations console. It contains rooms + the shared photo tray, with no always-visible Composer and no Jobs section. Image click opens the focused single-photo editor; corner selection reveals an explicit Edit photo/Edit N photos action and only then mounts the Composer. User-facing flow language is `Describe the edit` → `Build edit` → `Start edit`; `job`, `materialized`, and `run` remain internal vocabulary.
- 2026-08-31 (phase 35): Background work lives at `/listings/[id]/activity`. The activity treatment is hairline-separated editorial rows, not nested cards: one job-level human status (`Waiting`, `Editing`, `Ready`, `Needs attention`), useful before/output thumbnail, internal `REWORK` and raw enum labels hidden, raw grounding collapsed as formatted `Edit context`, and FileGroup pages return to Activity. Successful submission returns to Photos with a quiet Activity link.
- 2026-08-31 (phase 35 status truth): Both orange-wall requests shown as running were already `COMPLETED` at fal.ai (10.633s / 10.610s inference); localhost cannot receive the webhook, so the DB badges were stale. Reconciled both through the existing state machine. Activity now polls a new authenticated, listing-owned `/api/listings/[id]/reconcile` endpoint while active work exists; it uses the same `getStatus` / `completeStep` / `handleGenerationError` invariants and cannot sweep another user's listing. Production webhooks remain primary.
- 2026-08-31 (phase 36 UX contract): `UI-SPEC.md` is the implementation contract for the full golden path. The user chooses a PHOTO outcome first; common task controls are contextual; plain language is an alternate/accelerating path that still materializes the validated ordered edit chain before generation. Internal Job/FileGroup/provider vocabulary stays out of the primary UI. EdenSign informed the split controls/photo pattern only; its visual identity, credit model, and upload-per-tool structure were not copied.
- 2026-08-31 (phase 36 navigation): `/api/jobs` already returned the exact `fileGroupIds`; discarding them and returning to Photos was the main post-submit break. Successful edits now route to the first returned `/listings/[id]/f/[fileGroupId]`. A batch is still one FileGroup per photo and the result page derives sibling navigation from the shared Job. Activity is history/recovery, not the primary destination.
- 2026-08-31 (phase 36 progress truth): The FileGroup result workspace now calls the authenticated listing-scoped reconcile endpoint every five seconds only while active, in addition to narrow production realtime subscriptions. This brings localhost to the same orchestrator truth without inventing a second state machine. Processing keeps the source photo visible; rework keeps the last result visible while the new immutable version develops.
- 2026-08-31 (phase 36 language/prompt safety): Customer-facing UI uses American `Color` while the internal/catalog enum remains `COLOUR_CHANGE`. Public landing price claims were removed. Its compiled-edit demonstration imports and renders the named `GEOMETRY_INTERIOR` export verbatim; geometry preservation text is never duplicated or paraphrased inline.
- 2026-08-31 (phase 37 task selection): The Task Studio has ONE edit-selection path at a time. Empty state = six common outcomes plus an expandable list containing only additional tools; after selection = task-specific controls plus Add another edit. The native `+ Add edit…` dropdown and the free-text edit-type chip were redundant and are removed. Saved edits remain accelerators, not a second catalog.
- 2026-08-31 (phase 37 interior direction): Virtual staging defaults to `furnishing_level=light` for MLS and treats the model as a senior residential designer: identify envelope/focal point/camera sightline, protect roughly 36-inch primary and 30-inch secondary circulation, use the minimum functional furniture set, and omit optional pieces before shrinking or crowding them. Living rooms explicitly keep the fireplace/view open and prohibit a sofa back across the central camera sightline. The verbatim `GEOMETRY_INTERIOR` sentence remains byte-for-byte unchanged.
- 2026-08-31 (phase 37 reference roles + QA): Auto-attached floor plans are spatial evidence, never style references. `refUrlsFor` deterministically sends photo/floor-plan refs before sample/style refs and grounding tells the prompt how to interpret each role. Staging adds four named QA checks—scale/placement, circulation/access, focal composition, and function/restraint—and severe layout failures participate in the existing single corrective retry; no second QA call or schema change.
- 2026-08-31 (phase 37 result layout): Before/after uses the original image as the contained intrinsic frame, opens at a true 50/50 split, and makes the full image a native range target with visible keyboard focus. The action rail becomes side-by-side only at `xl`; narrower viewports stack it below the image rather than clipping it or creating horizontal scroll.
- 2026-09-01 (phase 38 design direction): The installed `apple-design` skill is product-design guidance, not a request to clone Apple. Listing Studio keeps its warm brass mark and identity, but the brittle 3px-square system and decorative three-font application hierarchy are superseded by calm warm surfaces, a deliberate 10–18px radius scale, restrained depth, and substantially fewer borders. The photo remains the dominant object.
- 2026-09-01 (phase 38 typography/performance): Product UI uses the platform system font stack. Removed the `next/font` Google imports and runtime variables for Cormorant Garamond, Public Sans, DM Sans, and JetBrains Mono; hierarchy now comes from weight, size, tracking, leading, spacing, and material. The public landing inherits the same foundation rather than carrying a second application type system.
- 2026-09-01 (phase 38 studio interaction): The focused studio is canvas-left/control-dock-right from 768px and photo-first when stacked on mobile. Common outcomes are a direct quiet list with the full catalog behind disclosure. Stage keeps Room type as a selector but exposes Furniture style, Furnishing level, and Showcase as visible `aria-pressed` choices so the high-value decisions do not hide inside dropdowns.
- 2026-09-01 (phase 38 motion/accessibility): Feedback uses CSS transforms, opacity, and color with immediate pointer-down response; no ornamental animation dependency was added. The before/after comparison has an explicit Arrow/Home/End keyboard handler because browser QA found the harness did not move the native range reliably. Reduced-motion, reduced-transparency, increased-contrast, and visible-focus fallbacks are part of the global interface contract.
- 2026-09-01 (phase 38 scope): This is a presentation and interaction-system pass only. No prompt text, provider selection, API contract, orchestration transition, schema, storage behavior, pricing, or edit-catalog value changed.
- 2026-09-01 (phase 39 select contract): `components/ui/select.tsx` is now a Radix Select adapter, not a native select. It deliberately parses the existing `<option>`/`<optgroup>` children so the repository did not need a risky all-at-once call-site rewrite; controlled/default values, form `name`/`required`/`disabled`, and the existing `onChange(e.target.value)` contract remain intact. Radix's hidden bubble select preserves server-action form submission (browser-confirmed `room_type=other`, `units=ft`). Empty-value options become the trigger placeholder because Radix reserves empty item values. Optional `data-description` renders secondary popover copy.
- 2026-09-01 (phase 39 disclosure accessibility): The shared Disclosure uses Radix Collapsible with force-mounted content so CSS can animate measured grid height without a JS layout loop. Force-mount initially exposed closed form controls to the accessibility tree; the browser snapshot caught it. Closed content now pairs zero-row/opacity with `visibility:hidden` and `pointer-events:none`, delayed until the 200ms exit finishes; opening restores visibility immediately. This keeps the transition and removes hidden controls from focus/navigation.
- 2026-09-01 (phase 39 task continuity): Common tasks stay in one persistent compact rail. A measured absolute active surface moves between triggers; More tools opens from its source in an anchored popover. Each task's first-step options are cached in a ref when switching, so returning Stage after Dusk preserves the user's room/style/level/showcase instead of silently resetting work. Secondary chained edits remain separate and do not become mode drafts.
- 2026-09-01 (phase 39 visual staging): Furniture styles use deterministic local material/mood illustrations built from the exact palette/material language already in `FURNITURE_STYLES`, not remote stock photos and not generated example rooms. Remote photos would add loading/licensing drift and could be mistaken for promised output composition; these swatches communicate style quickly while labels remain authoritative. The prompt catalog itself is untouched.
- 2026-09-01 (phase 39 hierarchy and motion): Native details/select chrome is gone from application source. Disclosure and mode entry use restrained 160–200ms opacity/translate/height transitions; the global reduced-motion rule collapses them to 1ms. Mobile task overflow stays touch-scrollable but hides its native scrollbar. Processing/rework truth is rendered over the image as dark translucent material chrome; completed result utilities use one outer floating rail with spacing instead of nested cards. No gesture or animation dependency was added.
- 2026-09-01 (phase 39 scope): No imaging prompt, provider, interpreter, API, state-machine transition, storage path, schema, price, or edit-catalog value changed. This phase completes the interaction/component language only.
- 2026-09-01 (phase 40 staging previews): Phase 39's deterministic CSS furniture illustrations were an interaction prototype, not the final visual standard. They are superseded by nine locally bundled, AI-generated photoreal interior vignettes because staging style is a visual/material decision and the schematic sofa/lamp language read as cartoonish. The visible style name remains authoritative; previews use empty alt text and communicate mood/materials rather than promising the exact generated furniture composition.
- 2026-09-01 (phase 40 asset policy): The staging previews are generated project assets compressed to WebP and served from `public/staging-styles/`, not remote stock imagery. This retains the original no-remote-dependency benefit without accepting illustrative quality: no third-party uptime, tracking, hotlink, or stock-license drift. Modern, Contemporary, and Farmhouse were generated as separate art-directed images after the first shared contact sheet left those categories too ambiguous; do not claim visual taxonomy is exact when adjacent styles are not clearly distinguishable.
- 2026-09-01 (phase 41 asset source): Phase 40's generated staging previews are superseded by source-verified real Pexels photography. Pexels was selected because the current license permits commercial app use/modification and its contributor policy rejects generative-AI uploads. The downloaded/cropped WebPs remain local assets; the picker never hotlinks, calls Pexels at runtime, or passes the previews to fal.ai/Anthropic as model references. Provenance lives beside the files in `public/staging-styles/SOURCES.md`.
- 2026-09-01 (phase 41 curation gate): A style preview must communicate its label through at least three usable interior signals—furniture form, palette, materials/detailing, or architectural language—at the actual 480 × 270 crop. Search tags alone are insufficient. Reject people, logos, prominent art/personal imagery, product-like close-ups, architecture-only compositions, and adjacent-style ambiguity. The visible label remains authoritative even after review approval.
- 2026-09-01 (phase 41 final taxonomy): The first candidate set retained only Modern, Mid-Century Modern, and Hamptons. Six mappings were replaced; the strict final check required a second Industrial crop with brick/leather/steel/timber room context and a different Scandinavian source with pale timber, functional white furniture, and a graphic Nordic rug. All nine final mappings passed the interior-staging review; this is curated visual guidance, not a promise of the exact generated furniture or room composition.
- 2026-09-01 (phases 38–41 production release): GitHub `main` was advanced by 15 verified commits through `feea1cd` and Vercel production deployment `dpl_BQqSg9iYha9UZ1vsWqwYHSfAoBUV` was promoted to `https://listing-studio-three.vercel.app`. Vercel reported Ready after its own clean Next.js build. Public `/` and `/login` return 200; unauthenticated `/dashboard` returns the intended 307 to `/login`. Staging preview paths also return the intended authentication redirect without a session because middleware protects application assets; signed-in studio requests load the locally bundled files.
- 2026-09-01 (post-phase 41 UX direction): Freeze the deployed Phase 38–41 interface before further visual changes. The next work is a competitive end-to-end UX audit, not another aesthetic sweep: compare the same upload→organize→edit→batch→progress→review→rework/version→delivery journey across direct real-estate editors and strong adjacent creative workflow products, then rank findings as `steal / adapt / reject`. Research must precede implementation, and Phase 42 must be appended to `PLAN.md` before the audit begins. The known >10 MB upload failure is a higher-priority UX risk than remaining font, motion, or radius polish.
- 2026-09-01 (phase 42 research verdict): Keep the Phase 38–41 visual system frozen. Competitive evidence does not justify another typography, radius, animation, color, material, or staging-thumbnail sweep; reopen only the workflow surfaces where the audit found task failure or unsafe ambiguity: intake/queue, organization, batch scope, listing-level proofing/status, and delivery.
- 2026-09-01 (phase 42 P0 intake): The known >10 MB request truncation is P0 rank 1 and must precede every further interface change. A copy-only error improvement is rejected: the future intake contract must provide bounded published limits, per-file/direct-resumable transport, type/size preflight, individual progress, pause/cancel/retry, and partial success while preserving authenticated listing ownership and immutable originals. Exact implementation remains subject to Matt's phase approval.
- 2026-09-01 (phase 42 P0 scope/finals): Processing completion is not delivery approval. Future batch staging must expose exact scope and block or split untagged/mixed-room selections; future delivery must use one explicitly approved version per source photo, not the latest version of every completed FileGroup, and must produce deterministic MLS/client-facing filenames. This is a research recommendation recorded for approval, not authorization to implement.
- 2026-09-01 (phase 42 sequencing): Recommended implementation order is reliable intake → shoot organization/HDR/rooms → safe batch scope plus named persistent presets → listing-level progress/proofing → approved finals and MLS delivery → variation/rework upgrades → mobile intake/state hardening. No implementation phase is appended until Matt chooses the scope and order.
- 2026-09-01 (post-phase 42 roadmap): Matt approved converting the audit direction into an implementation roadmap. The final one-context sequence is Phase 43 secure intake contract; 44 resumable queue; 45 inventory/HDR; 46 room/same-view proposals; 47 safe batch; 48 presets; 49 progress; 50 proofing/finals; 51 delivery; 52 versions/variations; 53 scoped rework; 54 mobile/recovery. Planning the arc does not authorize running all phases, applying live migrations, paid generations, pushes, or deployment.
- 2026-09-01 (phases 43–54 clear/resume): Execute one phase per context. Every phase starts after `/clear` by reading `CLAUDE.md`, the first `ACTIVE HANDOFF`, newest decisions, and its PLAN block; every interruption/completion leaves a coherent verified commit, exact PROGRESS handoff, migration/deploy/port state, clean worktree, and next resume seed. Never build while port 3000 runs and never switch ports.
- 2026-09-01 (phase 43 intake architecture plan): Use a private mutable intake area plus deterministic/idempotent finalization, path-scoped storage ownership, and explicit source/canonical paths. Exact uploaded bytes remain the immutable source original; HEIC/orientation normalization creates a separate immutable working derivative. Initial planned limits are 50 MB/file and 100 files/selection. Server-side object/type/size/ownership verification and Storage/Postgres crash-boundary tests are required before queue UI work.
- 2026-09-01 (phase 45 HDR identity plan): A confirmed bracket group preserves every source exposure but exposes exactly one merged representative to batch selection, proofing, approval, counts, and delivery. Source exposures remain inspectable/reversible and are never silently deleted or double-counted as finals.
- 2026-09-01 (phase 47 batch safety plan): Existing Shift-range selection is preserved and made discoverable; new work is Select all/group/mobile scope and an immutable server-validated target snapshot. `/api/jobs` must reject mixed/untagged staging without explicit per-target room chains/overrides even when a crafted client bypasses UI safeguards.
- 2026-09-01 (phase 51 delivery architecture plan): `Ready` never means approved. Phase 50 disables the current latest-completed-output ZIP; Phase 51 replaces it with approved-only package preview and a backpressured streaming archive or durable asynchronous artifact. Whole-shoot JSZip buffering, implicit latest-version fallback, unsafe filenames, and implicit scope are rejected.
- 2026-09-01 (roadmap migration reservations): Planned migration slots are 0009 intake, 0010 shoot/HDR organization, 0011 room proposals, 0012 batch scope, 0013 presets, 0014 proofing/finals, 0015 delivery profiles, 0016 version labels, and 0017 scoped rework. They are reservations only; execution may alter a slot only with the deviation recorded before dependent work.
- 2026-09-01 (phase 43 source/canonical contract): Professional intake has two explicit identities. `source_storage_path` is the immutable byte-for-byte upload; `photos.storage_path` remains the canonical browser/model path. Correctly oriented JPG/PNG/WebP and floor-plan PDFs may share those paths. HEIC/HEIF and EXIF-rotated images retain the untouched source and receive a separate deterministic immutable canonical derivative. A temporary insert trigger defaults source to canonical for legacy callers until Phase 44 removes the multipart listing path.
- 2026-09-01 (phase 43 trust boundary): Clients receive server-generated, non-upsert signed tokens for one deterministic `intake` path after listing/room ownership and centralized 50 MB/file, 100-file/selection validation. Storage policies are no longer bucket-wide: authenticated access is restricted to the first path segment matching `auth.uid()`, originals have no authenticated update/delete policy, and upload batch/item tables are authenticated-read-only. Privileged lifecycle mutations happen only after a fresh RLS-owned read in server routes.
- 2026-09-01 (phase 43 finalization): Storage writes use deterministic immutable paths before one Postgres function atomically inserts exactly one photo and completes its upload item. The function is executable only by `service_role`, requires the server-set `finalizing/complete` state, revalidates declared size/type and user/listing/photo path shape, and rejects identity conflicts. This closes the phantom-photo bypass that an authenticated-callable finalizer would permit while preserving retry convergence after copied source, written canonical, lost response, or row-created/status-not-finished boundaries.
- 2026-09-01 (phase 43 cancellation/cleanup): Cancellation is allowed only from `reserved/failed`; it returns conflict once finalization begins so cleanup cannot race a new immutable copy. It removes intake and any uncommitted deterministic originals only after confirming no photo row exists. Completed finalize retries also retry mutable-intake cleanup. Finalized originals remain undeletable/unoverwritable to authenticated users; service-role removal is reserved for explicit harness artifact cleanup and future controlled retention work.
- 2026-09-01 (phase 43 live recovery): The live harness deliberately rewrote a completed item to `finalizing` after its intake object had been cleaned, exposing that a retry which always rematerialized from intake could not recover row-created/status-not-finished state. Finalize now reuses already-validated source/canonical metadata when a `finalizing` item has it, then re-enters the atomic RPC. Fresh/incomplete items still require the staged object and full size/magic-byte validation. The 42.1 MB TUS, double-finalize, lost-status, cleanup, EXIF, and HEIC cases all pass after this correction.
- 2026-09-01 (phase 43 RLS verification semantics): Supabase Storage may report a successful delete request when RLS matched no object row. Security tests therefore assert the material guarantee—not the response shape—by downloading through the service role after authenticated overwrite/delete attempts and comparing the source SHA-256. The live original remained present and byte-identical; cross-prefix reads/writes and direct authenticated finalizer execution were also denied.
- 2026-09-01 (phase 43 completion boundary): Migration `0009_reliable_intake.sql` is live and backward compatible, but Phase 43 does not claim the visible >10 MB UX is fixed: the frozen listing UI still uses `/api/upload`. Phase 44 alone moves photo/floor-plan callers to the proven TUS contract and must supply progress, pause/resume, cancel, retry, reload recovery, and partial success before the P0 can be considered closed for users.
- 2026-09-01 (phase 44 direct-upload authentication): Supabase's current documented 6 MiB TUS chunk requirement remains the implementation contract. A live browser probe showed that this project's signed `x-signature` token alone returned `Invalid Compact JWS`; matching the already-proven Phase 43 harness by also sending the current authenticated bearer succeeds. Client `getSession()` is used only to forward the raw credential to Storage, which performs authorization; no client-side ownership decision trusts session claims.
- 2026-09-01 (phase 44 concurrency and recovery): The three-file limit covers each file's transfer-through-finalize pipeline and is enforced by an in-memory active-ID set, not by rendering `Waiting` rows. TUS fingerprints include the durable upload-item ID so same-named shoot files cannot share a URL. Browser persistence stores reservation metadata, signed capability, progress, and TUS URL but never claims to persist `File` bytes; after reload, an interrupted item requires exact filename plus byte-size reselection, renews signed access, and resumes the stored URL. Finalizing items automatically re-enter the idempotent server finalizer.
- 2026-09-01 (phase 44 partial-success language): Client preflight happens per file before batch reservation. Invalid files become local `Needs attention` rows while valid neighbors reserve and continue; terminal server work is never reset by a later error. Failure copy names the file, says that chunks/completed neighbors are preserved, and gives Retry, exact-file selection, or cancel as the next action. Raw TUS responses, tokens, buckets, and request IDs are deliberately suppressed from customer-facing copy.
- 2026-09-01 (phase 44 durable recovery surface): `GET /api/uploads?listingId=…` returns only open batches after authenticated listing ownership, so a browser with no local queue can still recover reserved/failed/finalizing rows. `POST /api/uploads/[itemId]/authorize` renews signed access only for owned `reserved/failed` items. Server-absent nonterminal browser rows are discarded; uploaded/canceled rows remain locally until the user clears finished history.
- 2026-09-01 (phase 44 legacy-route boundary): Listing photo and floor-plan selection no longer call multipart `/api/upload`. The route is retained narrowly for the Aerial annotator's generated small flattened PNG and the legacy 360 harness; neither is professional full-shoot intake. Moving or deleting that internal caller is deferred rather than pulling specialty-tool refactoring into Phase 44.
- 2026-09-01 (phase 44 completion boundary): The >10 MB intake P0 is closed in verified local code: a 50-file selection achieved 49 independent successes plus one invalid preflight row, a 42.1 MB file paused/reloaded/resumed to exactly one photo, HEIC kept source plus canonical derivative, and PDF retained floor-plan identity. Production remains unchanged until an explicitly approved push/deploy; Phase 45 begins organization/HDR and must preserve this queue contract.
- 2026-09-01 (phase 45 metadata identity): Intake order and source batch are explicit photo lineage, not inferred later from timestamps. Capture time, shutter, exposure bias, aperture, ISO, focal length, camera, and lens are extracted once from the immutable source during finalization. Camera timestamps without an EXIF offset retain the raw value in `source_metadata` and use a documented UTC assumption only for relative bracket ordering; the UI must not present that assumption as a known timezone.
- 2026-09-01 (phase 45 detector boundary): Automatic HDR proposals require 3–9 adjacent frames from the same intake batch, known capture timing with at most 2.5 seconds between frames, matching dimensions/camera/lens/focal context, and either at least three exposure settings spanning 1 EV or a low-confidence brightness span of at least 0.12 when exposure EXIF is incomplete. Brightness never groups an untimed folder. Low-confidence proposals remain review-only.
- 2026-09-01 (phase 45 reversible lineage): A confirmed HDR group owns one current merged representative and hides only its source members from new downstream selection. Reopening or changing members allocates a new immutable merge identity; prior merged derivatives remain traceable and are not deleted or silently reused. Dismissing marks the exposures as reviewed singles so the detector does not immediately recreate the rejected proposal; manual grouping can still override that decision.
- 2026-09-01 (phase 45 review evidence): Detector confidence/reason describes only the detected membership. Any persisted manual reorder/add/remove changes the group to `Manual` confidence and replaces the detector sentence with “Adjusted manually from the reviewed source exposures.” This prevents a split six-frame group from continuing to claim it contains nine detected frames.
- 2026-09-01 (phase 45 count language): `Source photos` means immutable uploaded non-attachment exposures; `Photos ready` is the logical downstream set; `Floor plans` stays separate; `Merged results` counts only current confirmed representatives. The dashboard leads with logical photos, then floor plans, and shows source files when that number differs. Attachments and hidden bracket members never inflate the primary photo count.
- 2026-09-01 (phase 45 completion boundary): Migration `0010_shoot_organization.sql` is live and the source/group contract is browser-proven. Phase 45 does not infer rooms, auto-run HDR on upload, delete bracket sources, ingest RAW, or apply generation edits. Phase 46 may analyze only the logical representative set and must keep the human authoritative over room/same-view semantics.
- 2026-09-01 (phase 46 proposal/commit boundary): Model output is durable review evidence, not accepted listing semantics. Analysis may write `room_analysis_runs` and current `room_proposals`; only an explicit reviewed decision may write `photos.room_id`, create a dimensionless room, or add durable `same_room_group_members`. Deferred proposals persist as `Untagged` without changing the photo, and prior runs remain historical when a deliberate rerun replaces the current proposal set.
- 2026-09-01 (phase 46 logical-photo enforcement): The Phase 45 logical representative rule is duplicated at the database mutation boundary, not trusted to the browser. Floor plans, confirmed HDR source members, and non-current merged derivatives cannot be accepted or linked even through a crafted request. Same-room membership additionally requires one confirmed room, and unlinking below two useful angles removes the group rather than preserving a misleading singleton scope.
- 2026-09-01 (phase 46 confidence/model contract): Room analysis uses the existing cheapest adequate vision-capable Claude Haiku 4.5 interpreter tier under the distinct `ROOM_ANALYSIS` ledger label. `0.8` is the high-confidence preselection threshold; `other`, malformed, missing, and uncertain proposals remain reviewable, and singleton same-room keys are discarded. The prompt may match an explicitly supplied existing-room ID but may never infer dimensions, plan geometry, adjacency, hidden openings, furniture, or acceptance.
- 2026-09-01 (phase 46 ledger ownership): `spend_ledger` gains direct nullable `listing_id` and `room_analysis_run_id` roots because room organization occurs before any generation Job exists. Its existing Job-rooted records remain unchanged; owned ledger reads accept either root. This avoids manufacturing a fake generation Job solely to make analysis spend visible and auditable.
- 2026-09-01 (phase 46 analysis transport): A full shoot is reduced to labeled 4 × 3 contact sheets rather than sending up to 100 full-resolution image blocks. Tile labels map back to the exact logical-photo ID order, unreadable/missing tiles become explicit partial failures, and the prompt sees only the readable ID sequence. This bounds request size while preserving cross-sheet same-room comparison in one model call.
- 2026-09-01 (phase 46 proposal publication): Deliberate analysis writes a running/idempotent run before model work, ledgers any incurred tokens, inserts parsed proposals as non-current history, and only then switches the listing's current proposal set. Failed/unreadable responses preserve the prior review set and every photo remains usable. Valid rows survive malformed neighbors; partial state names the manual-review remainder.
- 2026-09-01 (phase 46 browser-found cleanup fix): The original `0011` no-key acceptance branch deleted every singleton group in the listing, which could erase a first accepted angle before its partner was reviewed. Cleanup is now scoped only to the group the current photo left. The authenticated route uses temporary per-proposal singleton keys plus targeted cleanup so the already-live `0011` function behaves safely; `0012_room_proposal_cleanup.sql` permanently replaces the function for Matt's deferred SQL batch.
- 2026-09-01 (roadmap migration reservation shift): Phase 46 consumed `0012` for the browser-found cleanup correction. Remaining reservations shift by one: Phase 47 batch scope `0013`, Phase 48 presets `0014`, Phase 50 proofing/finals `0015`, Phase 51 delivery profiles `0016`, Phase 52 version labels `0017`, and Phase 53 scoped rework `0018`. The earlier reservation entry remains historical, not current.
- 2026-09-01 (phase 46 completion boundary): Room analysis is explicit, human-reviewed organization only. It may suggest an existing room and same-view identity but never writes dimensions, plan geometry, adjacency, edits, furniture consistency, or hidden-photo behavior. Deterministic browser QA proved 6 accepted (including one corrected), 1 deferred, two reversible two-view groups, reload persistence, floor-plan/no-floor-plan behavior, and zero phone overflow; no paid model call, push, deploy, or production release occurred.
- 2026-09-01 (phase 46 live cleanup migration): Matt confirmed `0012_room_proposal_cleanup.sql` ran successfully after the Phase 46 completion checkpoint. The live database function now performs targeted prior-group cleanup directly; migrations are current through `0012`, and Phase 46 has no remaining SQL gate.
